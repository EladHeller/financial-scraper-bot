export interface AccountData {
    accountName: string;
    balance: number;
    lastUpdated: Date;
  }
  
 export interface ScraperConfig {
    username: string;
    password: string;
    baseUrl: string;
  }
